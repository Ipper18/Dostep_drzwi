<p style="color: green"><%= notice %></p>

<h1>Rooms</h1>

<div id="rooms">
  <% @rooms.each do |room| %>
    <%= render room %>
    <p>
      <%= link_to "Show this room", room %>
    </p>
  <% end %>
</div>

<%= link_to "New room", new_room_path %>

<div>
  <%= link_to 'Powrót do Panelu Sterowania', dashboard_path, class: 'btn btn-primary' %>
</div>
